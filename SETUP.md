# SETUP.md

## ⚠️ プロジェクト開始前の必須手順（Gate 0）

> **このファイルを読むタイミング**: このリポジトリをクローンした直後、プロジェクト開始時に一度だけ。
> `CLAUDE.md` の毎セッション読み込みリストには含まれない — Step 1〜3が完了しGate 0〜3を通過した後は、
> 日常作業においてこのファイルを再読する必要はない（Gate 4は機能・UC単位で繰り返されるが、
> そのサイクルは `.claude/rules/30-testing.md` と `/tdd` コマンドが駆動するものであり、
> このファイルの再読とは無関係）。

このリポジトリをクローンしたら、コードに触れる前に以下を順番に埋めること。
AIはこれらのファイルが埋まっていない状態では正確な支援ができない。

### Step 0 — 導入タイプの判定

| 状況 | パス |
|---|---|
| 新規プロジェクト——既存コードがなく、要件もまだ書かれていない | 以下の**Step 1**へ進む（変更なし） |
| 既存コードベース——すでに動いているコードを持つプロジェクトに本ハーネスを導入する（本リポジトリでは「ブラウンフィールド導入」とも呼ぶ。`meta/adr/ADR-0011` 参照） | 以下の**既存コードベース導入パス**へ進み、その後**Step 4**に合流する |

両パスともStep 4（TDD: Red → Gate 4 → Green → Refactor）は共通であり、異なるのはGate 0〜3の入力（`docs/ai-context/`、スタック関連ADR、`use-cases.md`、`data-model.md`）がどう作られるかだけである。理由の詳細は `meta/adr/ADR-0012-existing-codebase-adoption.md` を参照。

> この判定は毎回この表を読んで手動で行う必要はない。`docs/ai-context/project-summary.md` がプレースホルダーのままで、かつリポジトリに既に実質的なアプリケーションコードが存在する場合、このファイル自身が——他の依頼に着手する前に——既存コードベース導入の可能性を提案する。このファイルは他の何を尋ねられたセッションであっても必ず最初に読まれるため。

### Step 1 — フロントエンド技術選定 → ai-context を埋める（最初に必ずやること）

**1a. フロントエンド技術選定**

- `meta/adr/ADR-0005-frontend-stack.md` の選定基準・比較表を確認し、このプロジェクトのフロントエンドスタックを決定する
- `/adr` コマンドで選定結果を `docs/adr/ADR-XXXX-frontend-stack-selection.md` として記録する（デフォルト推奨〔Vue 3 + Inertia.js + Pinia〕以外を選ぶ場合、または複数候補で迷った場合は理由と却下案を明記する）

**1b. 選定確定後のルールファイル反映**

- `.claude/rules/15-frontend.md` の内容を選定結果に合わせて書き換える（Vue 3 + Inertia.js + Pinia を選定した場合はデフォルト内容のまま利用可）
- `docs/ai-context/module-map.md` の Frontend セクションを実際のディレクトリ構成に書き換える（例示のままにしない）

**1c. ai-context を埋める**

> 作成にあたっては `docs/original-docs/` に一次資料（要件メモ・画面スケッチ等）を置いてから参照すること。
> Step 1 完了後は `docs/original-docs/` をデフォルト参照先としない。
> `project-summary.md` の Frontend 行には 1a の選定結果を転記する。

| ファイル | 内容 | 優先度 |
|---|---|---|
| `docs/ai-context/project-summary.md` | プロジェクト全体の概要・目的・技術スタック | 必須 |
| `docs/ai-context/glossary.md` | プロジェクト固有の用語・略語 | 必須 |
| `docs/ai-context/module-map.md` | ディレクトリ構成と各モジュールの責務 | 必須 |
| `docs/ai-context/do-not-touch.md` | AIが変更してはいけない領域・ファイル | 必須 |
| `docs/ai-context/common-commands.md` | よく使うコマンド（migrate / test / lint 等） | 推奨 |

### Step 2 — 要件定義ドキュメントを作成する

```
docs/product/requirements.md        ← ビジネスチーム・BAが作成
    ↓ Gate 1: レビュアー承認
docs/product/use-cases.md           ← ビジネスチーム・BAが作成（AIによる叩き台生成可）
    ↓
docs/product/mockups/               ← AIによる叩き台生成可（/generate-mock コマンド利用）
    ビジネス側レビュー → フィードバックを use-cases.md に反映
    ↓ Gate 2: レビュアー最終承認 ★ここを通過するまでコード生成禁止
docs/product/acceptance-criteria.md ← AIによる叩き台生成可
```

> **モック作成タイミングの原則**: モックはGate 1通過後〜Gate 2の間に作成する。
> ビジネス側との要件認識合わせが目的であり、Gate 3（データモデル承認）を待つ必要はない。
> モックフィードバックをuse-cases.mdに反映してからGate 2承認を行う。

### Step 3 — アーキテクチャ設計

```
docs/architecture/data-model.md  ← 開発者が作成（AIによる叩き台生成可）
docs/architecture/overview.md    ← 開発者が作成
docs/adr/ADR-xxxx-[title].md     ← 技術選定の都度作成
    ↓ Gate 3: レビュアー承認
```

### 既存コードベース導入パス——すでに動いているコードを持つプロジェクトへの導入

> 本リポジトリの他箇所（`meta/adr/ADR-0011` 等）では「ブラウンフィールド導入」とも呼ぶ。
> プロジェクトに既にコードがある場合、Step 1〜3の代わりにこちらを使う。以下の統合レビューが
> 完了した時点でStep 4に合流する。

**基本原則**: 現在何が起きているかについては、動いているコードベースの方が正である。既存の社内ドキュメント（Wiki・古い仕様書・READMEなど）は突き合わせのための参考資料であり、実際のコードを読むことの代わりにはならず、コードがしていることを黙って上書きする根拠にもならない。**迷ったら「要確認」リストに載せること。** これはあらゆる状況を事前に分類し尽くそうとするルールブックではない——そのようなことをしても結局はユーザーと都度検討し直すことになるだけである。守るべき一点だけがある：曖昧さやコードとドキュメントの食い違いを、推測で解消して先に進めてはならない。必ず尋ねること。

**誰が何をするか**: `docs/development/ai-workflow.md` の「役割分担」に、新規プロジェクトと既存コードベース導入を並べた形で記載している。要約すると、AIが以下すべてをドラフトして2本のリスト（後述）をまとめ、人間はブロッキング側のリストを解消して1回の承認を行うだけであり、これらのドキュメントを手で書く必要はない。

**2本のリスト——ブロックするのは片方のみ**:
- **「要確認」**（ブロッキング）——ドラフトしたドキュメントが信頼できるかどうかに関わるもの：用語の意味の推測、do-not-touch境界の推測、use-casesの食い違い、ルール適用先を左右するスタック不一致。このリストを解消することが、そのままGate 0〜3の承認になる。
- **「Backlog」**（非ブロッキング、提案のみ）——ドキュメントではなくコード自体に既に存在する問題：`.claude/hooks/domain-boundary-check.sh --audit-all` の指摘。参考情報として提示するのみで、Step 4に進むための必須事項ではない。別途の追跡は不要——これらの指摘は`ADR-0011`により今後の`/review`のたびに自然に再浮上する。（組み込みスキル`security-review`は別物で、実際にコードを触る将来のPRで使うdiffスコープのツールであり、このリストには含めない。オンボーディング自体の差分はドキュメントのみであり、diffスコープのセキュリティチェックを実行しても引き継いだアプリケーションコードは見えないため）

**Claude Code組み込みの`/init`との関係**: ここで`/init`は実行しないこと——コードベースを走査して単一の汎用的な`CLAUDE.md`を（上書き）生成するため、本テンプレート固有の`CLAUDE.md`（Gate 0案内、「毎セッション読み込みリスト」等）を、それを知らない汎用的な形で潰してしまう。`/onboard-existing-codebase`は、同じ発想（「AIがコードを読んでドキュメントをドラフトする」）を本ハーネス向けに拡張・専用化したものと理解するとよい——単一の汎用`CLAUDE.md`ではなく本ハーネス固有のファイル形（`docs/ai-context/`の5点セット）を対象とし、バックエンド/DB/認証のスタック検出（`ADR-0001/0002/0003/0005`との突き合わせ）も行い、`.claude/hooks/domain-boundary-check.sh`と連携し、`/init`が一切踏み込まない`use-cases.md`（as-is挙動）と`data-model.md`の生成にまで、確信度に応じた「要確認」リストの仕組みのもとで踏み込む。

**最速の進め方**: `/onboard-existing-codebase` を実行すれば、以下のStep 1B〜3Bを一気通貫で行う。個別に手動実行・再実行したい場合のために各ステップも記載する。

**Step 1B — 実際のスタックを検出し、`docs/ai-context/` を逆生成する**
- コード（`composer.json`/`package.json`、マイグレーション、ルート、認証設定）から、実際のフロントエンド・バックエンドフレームワーク・DBエンジン・認証方式を検出する。`meta/adr/ADR-0001`/`ADR-0002`/`ADR-0003`/`ADR-0005`の前提が当然成り立つと決めつけない
  - テンプレートの前提と一致 → そのまま進める
  - 不一致（DBエンジンが異なる、認可がPolicy/Gateではなく手書きロールチェックである等）→ 「要確認」リストにnamed mismatchとして追加する。どちらか一方を黙って書き換えない
  - バックエンドがそもそもPHP/Laravel系ではない → その場で止め、このテンプレートの既存コードベース導入パスがこのコードベースには構造的に合わないと伝える
  - 検出したフロントエンド/バックエンドのスタックを `/adr` で記録する。Decisionの節には「選んだ」のではなく「検出した」内容を書く
- `.claude/hooks/domain-boundary-check.sh --audit-all` を実行する。その指摘は「要確認」リストではなく**Backlog**リストに載せる——これはまさに`ADR-0011`が既に想定している「積み残しであってブロッカーではない」ケースであり、既存コードのアーキテクチャ負債が、ここに来た本来の作業をブロックすることを防ぐ
- 実際にそこにあるものから `project-summary.md`・`module-map.md`・`glossary.md`・`common-commands.md`・`do-not-touch.md` をドラフトする。「要確認」リストに載るのは用語の意味の推測と不確かなdo-not-touch境界だけであり、機械的に読み取れる内容はどちらのリストにも載らない

**Step 2B — 現状の挙動を `use-cases.md` として文書化する（as-isであり理想形ではない）**
- 実際のコードパス（routes → controllers → policies）から `use-cases.md` をドラフトする。これは現状の挙動の記述であり、望ましい挙動の仕様ではないと明記する
- `requirements.md` はここでは任意——「まだ存在しないものをなぜ作るか」という本来の目的が、既に動いているコードには当てはまらない
- コードが既存の社内ドキュメントと食い違う場合は、「要確認」リストに**食い違いメモ**を追加する。どちらか一方を黙って解消してはならない——コードか仕様のどちらを変えるかは、`.claude/rules/00-global.md` の既存ルールに従い人間が判断する

**Step 3B — 実スキーマから `data-model.md` を抽出する**
- 実際のマイグレーション・DBスキーマから `data-model.md` を生成する。ほぼ完全に機械的な抽出作業であるため「要確認」リストに載ることは稀であり、Step 1Bで確認したDBエンジンが前提となる

**このパスにおけるGate 0〜3（4回ではなく1回の統合チェックポイント）**: 人間が「要確認」リストのみを解消する。その際、ai-contextの正確性・検出したスタック・`use-cases.md`の正確性・`data-model.md`の正確性のすべてを明示的にカバーする。この1回の承認がGate 0〜3をまとめて満たす。Backlogリスト（アーキテクチャ関連の指摘）は同時に提示されるが、承認対象には含まれない——読むかどうか、今対応するか後で対応するかは自由である。

### Step 4 — コード生成・実装（Gate 2・3 通過後のみ）

> 両パス（新規プロジェクト・既存コードベース）で共通の手順。

実装は `/tdd` コマンドで **TDD（Red → Green → Refactor）** で進める。

```
Red → [Gate 4: テストケース承認 ★実装(Green)着手禁止] → Green → Refactor → /review
```

> Gate 4 は Gate 0〜3（プロジェクトで1度だけ通過）と異なり、機能・UC単位でTDDサイクルのたびに繰り返す。
> フェーズごとの手順・サブエージェント構成・スキル実行タイミングは `.claude/rules/30-testing.md` を参照。
